package rules

import models.Incident
import models.GameState
import models.Recommendation

rule "Memory cleanup"
    agenda-group "CLEANUP"
    salience -100
    when
        $i: Incident($id: incidentId)
        $gs: GameState(incidentId == $id)
    then
        delete($i);
        delete($gs);
end